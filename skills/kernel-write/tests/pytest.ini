[pytest]
markers =
    judge: qualitative test using the LLM-as-judge (needs Ollama or OPENROUTER_API_KEY)
    e2e: drives the real kernel-write skill via `claude -p` (slow, costs tokens, opt-in via RUN_E2E=1)
